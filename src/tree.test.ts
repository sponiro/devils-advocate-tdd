import {describe, expect, test} from '@jest/globals';
import {tree} from './tree';

describe('tree', () => {

    test('test1', () => {
        // expect(tree(["", "", ""])).toBe(true);
    });
})