import {describe, expect, test} from '@jest/globals';
import {flower} from './flower';

describe('flower', () => {

    test('test1', () => {
        // expect(flower(0)).toBe(true);
    });
})