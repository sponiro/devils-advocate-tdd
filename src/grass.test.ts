import {describe, expect, test} from '@jest/globals';
import {grass} from './grass';

describe('grass', () => {

    test('test1', () => {
        // expect(grass([1, 2, 3], 1)).toBe(true);
    });
})