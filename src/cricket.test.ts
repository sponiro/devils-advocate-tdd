import {describe, expect, test} from '@jest/globals';
import {cricket} from './cricket';

describe('cricket', () => {

    test('test1', () => {
        // expect(cricket(["a", "b", "c"])).toBe(true);
    });
})