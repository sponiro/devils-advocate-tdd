import {describe, expect, test} from '@jest/globals';
import {cloud} from './cloud';

describe('cloud', () => {

    test('test1', () => {
        // expect(cloud("", 0)).toBe(true);
    });
})