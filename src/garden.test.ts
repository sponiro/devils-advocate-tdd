import {describe, expect, test} from '@jest/globals';
import {garden} from './garden';

describe('garden', () => {

    test('test1', () => {
        // expect(garden(0)).toBe(true);
    });
})