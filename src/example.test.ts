import {describe, expect, test} from '@jest/globals';
import {example} from './example';

describe('example', () => {

    test('test1', () => {
        // expect(example(0)).toBe(true);
    });

    // test('test2', () => {
        // expect(example(0)).toBe(true);
    // });
})